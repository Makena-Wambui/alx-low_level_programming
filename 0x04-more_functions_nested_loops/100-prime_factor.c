#include <stdio.h>
/**
 * main - this is the entry point of the program.
 * It finds and prints largest prime factor of a number.
 *
 * Return: 0
 */
int main(void)
{
unsigned long int n;
unsigned long int largest;

n = 612852475143;
largest = n;
printf("%lu", largest);
return (0);
}
